{
    "presets": ["@babel/preset-env", "@babel/preset-react", "@babel/preset-typescript"],
    "plugins": [
        [
            "./index",
            {
                // "libs": ["@tinper/next-ui", "tinper-bee"], // 可选，有默认值
                // "output": "./apiLog.json" // 可选，有默认值
            }
        ]
    ]
}
